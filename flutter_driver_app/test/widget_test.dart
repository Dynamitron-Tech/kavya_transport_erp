import 'package:flutter_test/flutter_test.dart';
import 'package:kt_driver_app/main.dart';

void main() {
  testWidgets('KTDriverApp builds', (WidgetTester tester) async {
    // Smoke test — just verify the app widget can be created
    expect(KTDriverApp, isNotNull);
  });
}
